function maiorComprimento(strA, strB) {
    if (strA.length == strB.length)
        return "Ambas tem o mesmo Comprimento, siga o Exemplo = " + "maiorComprimento(teste, teste string) = " + maiorComprimento(stringA, stringB);
    if (strA.length > strB.length)
        return console.log(strA)
    else
        return console.log(strB)
}